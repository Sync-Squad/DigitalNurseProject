services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "${PORT:-3001}:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 3000
